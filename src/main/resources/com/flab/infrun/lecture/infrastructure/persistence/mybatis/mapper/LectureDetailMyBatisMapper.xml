<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
  namespace="com.flab.infrun.lecture.infrastructure.persistence.mybatis.mapper.LectureDetailMyBatisMapper">

  <select id="findById" resultType="com.flab.infrun.lecture.domain.LectureDetail">
    SELECT ID
    , CHAPTER
    , NAME
    , LECTURE_ID
    , FILE_ID
    FROM LECTURE_DETAIL
    WHERE ID = #{id}
  </select>

  <insert id="save" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO LECTURE_DETAIL (CHAPTER, NAME, LECTURE_ID, FILE_ID) VALUES (#{chapter},
    #{name}, #{lectureId}, #{fileId})
  </insert>

</mapper>
